## 2024-05-22 - Skip to Main Content Link
**Learning:** Adding a "Skip to main content" link is a critical accessibility feature that is often overlooked. It requires a target with `tabindex="-1"` to ensure programmatic focus works consistently across browsers.
**Action:** Always check for bypass blocks in layout files and ensure the main content area is focusable.
